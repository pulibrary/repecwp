default: &default
  pool: 5
  timeout: 5000

development: &development
  adapter: mysql2
  encoding: utf8
  database: <%= ENV["lando_repecwp_database_creds_database"] || repec %>
  host: <%= ENV["lando_repecwp_database_conn_host"] %>
  port: <%= ENV["lando_repecwp_database_conn_port"] %>
  username: <%= ENV["lando_repecwp_database_creds_user"] || ENV["APP_DB_USERNAME"] || 'user' %>
  password: <%= ENV["lando_repecwp_database_creds_password"] %>
  pool: 5

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *development


production: &production
  adapter: mysql2
  encoding: utf8
  database: <%= ENV.fetch("APP_DB", "repec") %>
  host: <%= ENV.fetch("APP_DB_HOST", "host") %>
  username: <%= ENV.fetch("APP_DB_USERNAME",'user') %>
  password: <%= ENV.fetch("APP_DB_PASSWORD",'password') %>

staging:
  <<: *production
